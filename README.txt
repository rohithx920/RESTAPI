1)To create a project go to command promt and go to the project folder.
2)npm init -> will create package.json file(Hit enter to all)
3)dependencies can be added by using
	npm install express --save
 --> This above code will add the express to package.json


4)To run app on node serve we use node app.js

5) Adding gulp to watch for changes
 -> npm install gulp --save

6) To run gulp from command line to run gulp
	npm install gulp -g

7) gulp going to look for gulpfile.js

8) nodemon plugin is required
	npm install gulp-nodemon --save

9) to run we have to use 
	gulp

10) npm install mongoose --save
	-> it is going to take mongo data and convert into JSON object

11) To Connect Mongodb 
 	open new cmd promt and start mongo server
	using mongod command
->C:\MongoDB\Server\3.4\bin>mongo

12)type gulp in our project floder


   